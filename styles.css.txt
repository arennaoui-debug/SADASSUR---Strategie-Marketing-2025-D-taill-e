/* Configuration de Tailwind pour la police Inter (par défaut) et les couleurs */
:root {
    --primary-color: #0077B6; /* Bleu Assurance */
    --secondary-color: #00B4D8; /* Bleu Clair */
    --accent-color: #48CAE4; /* Cyan */
}
.text-primary { color: var(--primary-color); }
.bg-primary { background-color: var(--primary-color); }
.bg-secondary { background-color: var(--secondary-color); }
.hover\:bg-accent:hover { background-color: var(--accent-color); }
.border-primary { border-color: var(--primary-color); }

/* Animation subtile pour l'entête */
@keyframes gradientPulse {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}
.header-bg {
    background: linear-gradient(-45deg, var(--primary-color), var(--secondary-color), var(--accent-color));
    background-size: 400% 400%;
    animation: gradientPulse 15s ease infinite;
}

/* Styles pour l'accordéon */
.accordion-content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94); /* Transition plus fluide */
}
.accordion-item.active .accordion-content {
    max-height: 2000px; /* Augmenté pour le contenu détaillé */
}
.accordion-item.active .accordion-icon {
    transform: rotate(180deg);
}

/* Animation au Scroll (Fade-in) - Utilisation d'une classe JS pour l'activation */
.fade-in-section {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.6s ease-out;
}
.fade-in-section.is-visible {
    opacity: 1;
    transform: translateY(0);
}

/* Styles pour les onglets (tabs) */
/* Le JS gère l'ajout/retrait des classes Tailwind, 
   mais nous pouvons garder la classe 'hidden' pour le JS */
.tab-pane.hidden {
    display: none;
}
.tab-pane.active {
    display: block;
}
